import 'package:flutter/material.dart';
class MainMenuItem{
  IconData icon;
  String title;
  MainMenuItem(this.icon,this.title);
}

class MovieDetails{
  String title;
  int year;
  MovieDetails(this.title,this.year);
}